<h4>CHIME IN WITH A RESPONSE</h4>

<%= form_for(@comment) do |c| %>
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% @comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div>
  <%= c.text_field :post_id, :value => @post.id %>
</div>

  <div>
  <%= c.label :name %>
  <%= c.text_field :name %>
</div>

<br>

<div>
  <%= c.label :body %>
  <%= c.text_area(:body, cols: 80, rows: 15)  %>
</div>

<br>

  <div class="actions">
    <%= c.submit %>
  </div>
<% end %>
